# Backend Server Configuration - Development

# Flask settings
DEBUG=True
PORT=5000

# ============================================================================
# DATABASE CONFIGURATION - POSTGRESQL REQUIRED
# ============================================================================
# CRITICAL: This application REQUIRES PostgreSQL (JSONB and ARRAY types)
# SQLite is NOT supported and will cause the server to fail on startup
#
# Get from: Supabase Dashboard → Settings → Database → Connection String
#
# For DEVELOPMENT (local PostgreSQL):
# DATABASE_URL=postgresql://postgres:password@localhost:5432/llm_search_behavior
#
# For PRODUCTION (Supabase - Transaction Mode for serverless):
# DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@[POOLER-HOST]:6543/postgres
#
# Connection Modes:
# - Session Mode (port 5432): Use for long-lived connections (local development, traditional servers)
# - Transaction Mode (port 6543): Use for short-lived connections (Vercel, AWS Lambda, serverless)
#
# Example (verify your region - could be aws-0-us-east-1, aws-1-us-east-2, etc.):
# DATABASE_URL=postgresql://postgres.abcdefghijklmnop:your-password@aws-1-us-east-2.pooler.supabase.com:6543/postgres
#
# CRITICAL: Copy the exact connection string from Supabase Dashboard - the pooler region varies by project!
DATABASE_URL=postgresql://postgres:password@localhost:5432/llm_search_behavior

# Supabase (for production)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your_anon_key
# SUPABASE_SERVICE_KEY=your_service_role_key

# Data storage
DATA_STORAGE_PATH=./data/sessions

# Supabase Storage (optional, for cloud file storage)
# STORAGE_BACKEND=supabase
# SUPABASE_BUCKET=session-data

# AWS S3 (optional alternative for cloud storage)
# STORAGE_BACKEND=s3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1

# Security (for production)
# SECRET_KEY=your-secret-key-here
# ALLOWED_ORIGINS=chrome-extension://your-extension-id
